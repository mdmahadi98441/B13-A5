(1st answer) : the difference between var, let, and const is 
   var: var old way of declaring variables , and var only maintain function scope.
       Can be re-declared and re-assigned.

    let: let maintain all of scope ,and can be re-assing not re-declared.

    const: const maintain all of scope , can't be re-declared and re-assigned.



2nd answer ::
       
       spread opareor is used to expand the elements of an array or object.


3rd answer::

     map filter এবং forEach() JavaScript array method
     map প্রতিটি element এর উপর operation করে এবং একটি নতুন array return করে।
     filter condition অনুযায়ী element select করে এবং নতুন array return করে।
     forEach array loop করার জন্য ব্যবহার হয় 


 4th answer::

     arrow function is ES6 mordern vartion in js it can code short and clear.


5th answer::

     Template literals are a modern way of writing strings in JavaScript using backticks ( ). They allow embedding variables and expressions inside strings using ${}.